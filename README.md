## 需要安装的环境

* python2
* python3
* java
* foremost

* zsteg

```bash
# ubuntu安装zsteg
apt install tcllib
apt install ruby
apt install gem
gem install zsteg
```

* pngcheck

```bash
apt install pngcheck
```

* stegpy

```
pip3 install stegpy
```

